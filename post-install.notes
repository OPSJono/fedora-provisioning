After finishing all provisioning.
Add any ssh keys to the /etc/ssh/ssh_config file. Look for IdentityFile and duplicate the line for as many keys as required. (uncomment the hosts *) line.
Probably move this to a file /etc/ssh/ssh.d/ or something that can be copied in as a template.